import { PrealoadDirective } from './preaload.directive';

describe('PrealoadDirective', () => {
  it('should create an instance', () => {
    const directive = new PrealoadDirective();
    expect(directive).toBeTruthy();
  });
});
